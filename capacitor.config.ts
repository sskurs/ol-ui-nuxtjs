import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.consumerapp',
  appName: 'consumer-app',
  webDir: './.output/public'
};

export default config;
